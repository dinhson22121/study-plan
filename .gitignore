.DS_Store
.idea/
.vscode/
.env
.env.*
!.env.example
*.local.yaml

# Coverage / build output
coverage.out
*.out

# TLS certs / keys — never commit (keep the dir via .gitkeep)
deploy/nginx/certs/*
!deploy/nginx/certs/.gitkeep
*.pem
*.key
